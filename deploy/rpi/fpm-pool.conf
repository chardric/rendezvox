[www]
; Raspberry Pi optimized — enough workers for SSE + admin requests
pm = dynamic
pm.max_children = 10
pm.start_servers = 3
pm.min_spare_servers = 2
pm.max_spare_servers = 5
pm.max_requests = 500
